class Player {
    constructor(x, y, width, height) {
        this.image = falcon;
        this.x = x;
        this.y = y;
        this.width = width;
        this.height = height;
      }
 
 }
 /*moveUp() {
    this.x = this.x + 15;
    if (this.x > 1000) {
        this.x = 0 - this.width;
    }
 }

 moveDown() {
    this.x = this.x - 15;
    if ( this.x + this.width < 0) {
        this.x = 1000
    }
 }

 shoot() {

 }*/
